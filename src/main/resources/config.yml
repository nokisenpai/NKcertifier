# NKcertifier
default-title: "c'est le default title"
default-subtitle: "c'est le default subtitle"

# Staff data
data:
  NoKi_senpai:
    password: "MyPassword"
    title: "MyCustomTitle"
    subtitle: "MyCustomSubtitle"

# Possible values : DEBUG, LOG, WARN, ERROR
log-level: LOG